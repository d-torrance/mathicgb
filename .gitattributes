# Normalize line endings to LF in the repository.  Without this, a commit
# made on Windows can reintroduce the CRLF endings cleaned up in f78d6be.
* text=auto

# PICT model and seed files are tab-delimited; leave them exactly as-is.
*.in  -text
*.seed -text
